# Core runtime
PyYAML>=6.0
tqdm>=4.66

# Medical image IO and processing
SimpleITK>=2.3
nibabel>=5.0
numpy>=1.24
scipy>=1.10

# Device detection for HD-BET skull stripping
torch>=2.0

# Radiomics extraction and table export
pyradiomics>=3.0
pandas>=2.0
openpyxl>=3.1

# External tools used by specific steps, not bundled in this repository:
# - HD-BET: the "hd-bet" command must be available for skull stripping.
# - FLAMeS / nnU-Net v2: configure run.flames.root in config.yaml.
# - SynthSeg: configure run.synthseg.python_executable and run.synthseg.script_path in config.yaml.
